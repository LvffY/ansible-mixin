install:
  - ansible:
      description: "Run our ansible adhoc command"
      adhoc: 
        arguments:
          - "localhost"
        flags:
          module-name: "debug"
          args: "msg='Hello from ansible AdHoc command !'"
